'use strict';

const utils = require('../lib/utils');

describe('Utils Module', () => {
  describe('#map', () => {
    it('should ...', () => {

    });

    it('should ...', () => {

    });
  });
});
